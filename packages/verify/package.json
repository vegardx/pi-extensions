{
  "name": "pi-ext-verify",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Plan verifier — fans out parallel read-only subagents to check whether each step of a plan is actually done.",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@vegardx/pi-extensions-shared": "*"
  },
  "peerDependenciesMeta": {
    "@mariozechner/pi-coding-agent": { "optional": true },
    "@vegardx/pi-extensions-shared": { "optional": true }
  }
}
